const { asyncHandler } = require("../Utils/AsyncHandler.Utils");




//same_randomized_ticket_number
//1.internal user should have same ticket it`s just the number placement of those tickets will be different

exports.same_randomized_ticket_number = asyncHandler(async () => {});
